# Base URL of the Artisan's Corner API.
# Leave as /api in development to use the Vite proxy.
VITE_API_URL=/api

# Stripe publishable key (pk_test_...). The server also returns this from
# /api/payments/config, so this is only a fallback.
VITE_STRIPE_PUBLISHABLE_KEY=
